# Screenshots

Add curated PNGs here so they render in the main README:

- `login.png` — the sign-in screen
- `model-picker.png` — model selection
- `training.png` — the live training view

Keep them reasonably sized (ideally < 500 KB each). PNG or WebP.
